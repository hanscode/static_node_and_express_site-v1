extends layout.pug

block content

  article.portfolio-intro

    h1 Let's make the web great together!

    p I'm a software engineer specializing in building (and occasionally designing) exceptional digital experiences. Currently, I'm focused on building accessible, human-centered digital products.


  article.portfolio-project-thumbs
    .projects-box.box.grid

      //- Using the Pug `each` iterator to loop over the projects in the data.json file  
      each project in projects
        .cell
          a(href=`/project/${project.id}`)
            img.thumbnail(src=`/static/images/project_${project.id}-main.jpg`, alt=`${project.project_name}`)
            h5=project.project_name

